import os

from setuptools import setup

reqs = ['matplotlib',
        'pandas',
        'scikit-learn',
        'seaborn',
        'statistics',
        'scipy',
        'numpy']


setup(name='Identify_Sucess_driver',
      version='0.0',
      zip_safe=False,
      install_requires=reqs)
